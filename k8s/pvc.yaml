apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: status-data-pvc
  labels:
    app: status
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
